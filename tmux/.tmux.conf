# : << 'EOF'
# Oh my tmux!
# https://github.com/gpakosz/.tmux
# (‑●‑●)> dual licensed under the WTFPL v2 license and the MIT license,
#         without any warranty.
#         Copyright 2012— Gregory Pakosz (@gpakosz).
#
# ------------------------------------------------------------------------------
# DO NOT MODIFY THIS FILE
#      instead, override your .local customization file copy, see README.md
# ------------------------------------------------------------------------------


# -- general -------------------------------------------------------------------

set -g default-terminal "screen-256color"

%if #{>=:#{version},3.2}
set -g extended-keys #{?#{||:#{m/ri:mintty|iTerm,#{TERM_PROGRAM}},#{!=:#{XTERM_VERSION},}},on,off} # needed by Control + Shift bindings
%endif
set -s escape-time 10                     # faster command sequences
set -sg repeat-time 600                   # increase repeat timeout
set -s focus-events on

set -g prefix2 C-a                        # GNU-Screen compatible prefix
bind C-a send-prefix -2

set -q -g status-utf8 on                  # expect UTF-8 (tmux < 2.2)
setw -q -g utf8 on

set -g history-limit 9999999                 # boost history

# edit configuration
bind e new-window -n "#{TMUX_CONF_LOCAL}" sh -c '_E=${VISUAL:-${EDITOR:-vim}}; case "$_E" in *vim*) $_E -c ":set syntax=tmux" "$TMUX_CONF_LOCAL";; *) $_E "$TMUX_CONF_LOCAL";; esac && "$TMUX_PROGRAM" ${TMUX_SOCKET:+-S "$TMUX_SOCKET"} source "$TMUX_CONF" \; display "$TMUX_CONF_LOCAL sourced"'

# reload configuration
bind r run "sh -c '\"\$TMUX_PROGRAM\" \${TMUX_SOCKET:+-S \"\$TMUX_SOCKET\"} source \"\$TMUX_CONF\"'" \; display "#{TMUX_CONF} sourced"


# -- display -------------------------------------------------------------------

set -g base-index 1           # start windows numbering at 1
setw -g pane-base-index 1     # make pane numbering consistent with windows

setw -g automatic-rename on   # rename window to reflect current program
set -g renumber-windows on    # renumber windows when a window is closed

set -g set-titles on          # set terminal title

set -g display-panes-time 800 # slightly longer pane indicators display time
set -g display-time 1000      # slightly longer status messages display time

set -g status-interval 10     # redraw status line every 10 seconds

# clear both screen and history
bind -n C-l send-keys C-l \; run 'sleep 0.2' \; clear-history

# activity
set -g monitor-activity on
set -g visual-activity off


# -- navigation ----------------------------------------------------------------

# create session
bind C-c new-session

# find session
bind C-f command-prompt -p find-session 'switch-client -t %%'

# session navigation
bind BTab switch-client -l  # move to last session

# split current window horizontally
bind - split-window -v
# split current window vertically
bind _ split-window -h

# pane navigation
bind -r h select-pane -L  # move left
bind -r j select-pane -D  # move down
bind -r k select-pane -U  # move up
bind -r l select-pane -R  # move right
bind > swap-pane -D       # swap current pane with the next one
bind < swap-pane -U       # swap current pane with the previous one

# maximize current pane
bind + run "cut -c3- '#{TMUX_CONF}' | sh -s _maximize_pane '#{session_name}' '#D'"

# pane resizing
bind -r H resize-pane -L 2
bind -r J resize-pane -D 2
bind -r K resize-pane -U 2
bind -r L resize-pane -R 2

# window navigation
unbind n
unbind p
bind -r C-h previous-window                             # select previous window
bind -r C-l next-window                                 # select next window
bind -r C-S-H swap-window -t -1 \; select-window -t -1  # swap current window with the previous one
bind -r C-S-L swap-window -t +1 \; select-window -t +1  # swap current window with the next one
bind Tab last-window                                    # move to last active window

# toggle mouse
bind m run "cut -c3- '#{TMUX_CONF}' | sh -s _toggle_mouse"


# -- facebook pathpicker -------------------------------------------------------

bind F run "cut -c3- '#{TMUX_CONF}' | sh -s _fpp '#{pane_id}' '#{pane_current_path}'"


# -- copy mode -----------------------------------------------------------------

bind Enter copy-mode # enter copy mode

bind -T copy-mode-vi v send -X begin-selection
bind -T copy-mode-vi C-v send -X rectangle-toggle
bind -T copy-mode-vi y send -X copy-selection-and-cancel
bind -T copy-mode-vi Escape send -X cancel
bind -T copy-mode-vi H send -X start-of-line
bind -T copy-mode-vi L send -X end-of-line

# copy to X11 clipboard
if -b 'command -v xsel > /dev/null 2>&1' 'bind y run -b "\"\$TMUX_PROGRAM\" \${TMUX_SOCKET:+-S \"\$TMUX_SOCKET\"} save-buffer - | xsel -i -b"'
if -b '! command -v xsel > /dev/null 2>&1 && command -v xclip > /dev/null 2>&1' 'bind y run -b "\"\$TMUX_PROGRAM\" \${TMUX_SOCKET:+-S \"\$TMUX_SOCKET\"} save-buffer - | xclip -i -selection clipboard >/dev/null 2>&1"'
# copy to Wayland clipboard
if -b '[ "$XDG_SESSION_TYPE" = "wayland" ] && command -v wl-copy > /dev/null 2>&1' 'bind y run -b "\"\$TMUX_PROGRAM\" \${TMUX_SOCKET:+-S \"\$TMUX_SOCKET\"} save-buffer - | wl-copy"'
# copy to macOS clipboard
if -b 'command -v pbcopy > /dev/null 2>&1' 'bind y run -b "\"\$TMUX_PROGRAM\" \${TMUX_SOCKET:+-S \"\$TMUX_SOCKET\"} save-buffer - | pbcopy"'
# copy to Windows clipboard
if -b 'command -v clip.exe > /dev/null 2>&1' 'bind y run -b "\"\$TMUX_PROGRAM\" \${TMUX_SOCKET:+-S \"\$TMUX_SOCKET\"} save-buffer - | clip.exe"'
if -b '[ -c /dev/clipboard ]' 'bind y run -b "\"\$TMUX_PROGRAM\" \${TMUX_SOCKET:+-S \"\$TMUX_SOCKET\"} save-buffer - > /dev/clipboard"'


# -- buffers -------------------------------------------------------------------

bind b list-buffers     # list paste buffers
bind p paste-buffer -p  # paste from the top paste buffer
bind P choose-buffer    # choose which buffer to paste from


# -- 8< ------------------------------------------------------------------------

%if #{==:#{TMUX_PROGRAM},}
  run "exec sh -c 'TMUX_PROGRAM=\"\$(LSOF=\$(PATH=\"\$PATH:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin\" command -v lsof); \$LSOF -b -w -a -d txt -p #{pid} -Fn 2>/dev/null | perl -n -e \"if (s/^n((?:.(?!dylib\$|so\$))+)\$/\\1/g && s/(?:\s+\\([^\\s]+?\\))?\$//g) { print; exit } } exit 1; {\" 2>/dev/null || readlink \"/proc/#{pid}/exe\" 2>/dev/null)\"; [ \"\$(\"\$TMUX_PROGRAM\" display -p \"#{l:#{pid}}\" 2>/dev/null)\" = \"#{pid}\" ] || TMUX_PROGRAM=\"\$(command -v tmux || printf tmux)\"; \"\$TMUX_PROGRAM\" -S #{socket_path} set-environment -g TMUX_PROGRAM \"\$TMUX_PROGRAM\"'"
%endif
%if #{==:#{TMUX_SOCKET},}
  run "exec sh -c '\"\$TMUX_PROGRAM\" -S #{socket_path} set-environment -g TMUX_SOCKET \"#{socket_path}\"'"
%endif
%if #{==:#{TMUX_CONF},}
  run "exec sh -c '\"\$TMUX_PROGRAM\" set-environment -g TMUX_CONF \$(for conf in \"\$HOME/.tmux.conf\" \"\$XDG_CONFIG_HOME/tmux/tmux.conf\" \"\$HOME/.config/tmux/tmux.conf\"; do [ -f \"\$conf\" ] && printf \"%s\" \"\$conf\" && break; done)'"
%endif
%if #{==:#{TMUX_CONF_LOCAL},}
  run "exec sh -c '\"\$TMUX_PROGRAM\" set-environment -g TMUX_CONF_LOCAL \"\$TMUX_CONF.local\"'"
%endif

run '"$TMUX_PROGRAM" -S #{socket_path} source "$TMUX_CONF_LOCAL"'
run 'cut -c3- "$TMUX_CONF" | sh -s _apply_configuration'

# EOF
#
# # exit the script if any statement returns a non-true return value
# set -e
#
# unset SHELL
#
# unset GREP_OPTIONS
# export LC_NUMERIC=C
# # shellcheck disable=SC3041
# if (set +H 2>/dev/null); then
#   set +H
# fi
#
# if ! printf '' | sed -E 's///' 2>/dev/null; then
#   if printf '' | sed -r 's///' 2>/dev/null; then
#     sed() {
#       n=$#; while [ "$n" -gt 0 ]; do arg=$1; shift; case $arg in -E*) arg=-r${arg#-E};; esac; set -- "$@" "$arg"; n=$(( n - 1 )); done
#       command sed "$@"
#     }
#   fi
# fi
#
# _uname_s=$(uname -s)
#
# [ -z "$TMUX" ] && exit 255
# if [ -z "$TMUX_SOCKET" ]; then
#   TMUX_SOCKET=$(printf '%s' "$TMUX" | cut -d, -f1)
# fi
# if [ -z "$TMUX_PROGRAM" ]; then
#   TMUX_PID=$(printf '%s' "$TMUX" | cut -d, -f2)
#   TMUX_PROGRAM=$(lsof -b -w -a -d txt -p "$TMUX_PID" -Fn 2>/dev/null | perl -n -e "if (s/^n((?:.(?!dylib$|so$))+)$/\1/g) { print; exit } } exit 1; {" 2>/dev/null || readlink "/proc/$TMUX_PID/exe" 2>/dev/null)
#   [ "$("$TMUX_PROGRAM" -S "$TMUX_SOCKET" display -p '#{pid}' 2>/dev/null)" = "$TMUX_PID" ] || TMUX_PROGRAM=$(command -v tmux || printf tmux)
# fi
# if [ "$TMUX_PROGRAM" = "tmux" ]; then
#   tmux() {
#     command tmux ${TMUX_SOCKET:+-S "$TMUX_SOCKET"} "$@"
#   }
# else
#   tmux() {
#     "$TMUX_PROGRAM" ${TMUX_SOCKET:+-S "$TMUX_SOCKET"} "$@"
#   }
# fi
#
# _tmux_version=$(tmux -V | perl -n -e '($l, $r) = $_ =~ /^[^0-9]+([0-9.]+)([a-z])?(-rc.*)?$/; print $l * 1000 + ($r eq "" ? 0 : ord($r) - 96)')
#
# _is_true() {
#   case "$1" in
#     true|yes|1)
#       return 0
#       ;;
#     *)
#       return 1
#       ;;
#   esac
# }
#
# _is_enabled() {
#   [ "$1" = "enabled" ]
# }
#
# _is_disabled() {
#   [ "$1" = "disabled" ]
# }
#
# _circled() {
#   circled_digits='⓪ ① ② ③ ④ ⑤ ⑥ ⑦ ⑧ ⑨ ⑩ ⑪ ⑫ ⑬ ⑭ ⑮ ⑯ ⑰ ⑱ ⑲ ⑳'
#   if [ "$1" -le 20 ] 2>/dev/null; then
#     i=$(( $1 + 1 ))
#     eval set -- "$circled_digits"
#     eval echo "\${$i}"
#   else
#     echo "$1"
#   fi
# }
#
# _decode_unicode_escapes() {
#   printf '%s' "$*" | perl -CS -pe 's/(\\u([0-9A-Fa-f]{1,4})|\\U([0-9A-Fa-f]{1,8}))/chr(hex($2.$3))/eg' 2>/dev/null
# }
#
# _pretty_path() {
#   max_length=${1:-auto}; shift
#   if [ "$max_length" = "auto" ]; then
#     max_length=$(($(tmux -q display -p '#{client_width}') / 4))
#   fi
#   { cd "$*" && pwd || printf '%s' "$*";  } | perl -s -p -E '
#     s|^$HOME|~|;
#     /^.{0,$max_length}$/ || s|(~?/[^/]+)(/[^/]+)(?:/.+)?(/[^/]+)$|$1$2/...$3|g;
#     /^.{0,$max_length}$/ || s|(~?/[^/]+)(/[^/]+)(?:/.+)(/[^/]+)$|$1/...$3|g;' -- -HOME="$HOME" -max_length="$max_length"
# }
#
# if command -v pkill > /dev/null 2>&1; then
#   _pkillf() {
#     pkill -f "$@" || true
#   }
# else
#   case "$_uname_s" in
#     *CYGWIN*)
#       _pkillf() {
#         while IFS= read -r pid; do
#           kill "$pid" || true
#         done  << EOF
# $(grep -Eao "$@" /proc/*/cmdline | xargs -0 | sed -E -n 's,/proc/([0-9]+)/.+$,\1,pg')
# EOF
#       }
#       ;;
#     *)
#       # shellcheck disable=SC2009
#       _pkillf() {
#         while IFS= read -r pid; do
#           kill "$pid" || true
#         done  << EOF
# $(ps -x -o pid= -o command= | grep -E "$@" | cut -d' ' -f1)
# EOF
#       }
#       ;;
#   esac
# fi
#
# if perl -MTime::HiRes -e1; then
#   _timestamp() {
#     while IFS= read -r line; do
#       printf '\t%s\n' "$line" | perl -MPOSIX=strftime -MTime::HiRes=gettimeofday -pe '($s, $us) = gettimeofday(); printf ("[%s.%03d]", strftime("%Y-%m-%d %H:%M:%S", localtime $s), $us / 1000)'
#     done
#   }
# else
#   case "$_uname_s" in
#     Darwin)
#       _timestamp() {
#         while IFS= read -r line; do
#           printf '[%s]\t%s\n' "$(date +"%Y-%m-%d %H:%M:%S.000")" "$line"
#         done
#       }
#       ;;
#     *)
#       _timestamp() {
#         while IFS= read -r line; do
#           printf '[%s]\t%s\n' "$(date +"%Y-%m-%d %H:%M:%S.%3N")" "$line"
#         done
#       }
#       ;;
#   esac
# fi
#
# _bar() {
#   bar_palette=$(printf '%s' "$1" | tr ';' ',')
#   bar_symbol_empty=$2
#   bar_symbol_full=$3
#   bar_length=$4       # "auto" or number of columns
#   bar_value=$5        # value between 0 and 1
#
#   if [ "$bar_length" = "auto" ]; then
#     columns=${6:-$(tmux -q display -p '#{client_width}' 2> /dev/null || echo 80)}
#     if [ "$columns" -ge 160 ]; then
#       bar_length=12
#     elif [ "$columns" -ge 130 ]; then
#       bar_length=10
#     elif [ "$columns" -ge 120 ]; then
#       bar_length=8
#     elif [ "$columns" -ge 100 ]; then
#       bar_length=6
#     else
#       bar_length=4
#     fi
#   fi
#
#   if printf '%s' "$bar_palette" | grep -q -E '^(heat|gradient|gradient\((\s*[#a-zA-Z0-9]{7,9},?)+\))(\s*,\s*[#a-zA-Z0-9]{7,9})?$'; then
#     # shellcheck disable=SC2086
#     { set -f; IFS=', '; set -- $bar_palette; unset IFS; set +f; }
#     palette_style=$1
#     shift
#
#     case "$palette_style" in
#       gradient)
#         palette="colour196 colour202 colour208 colour214 colour220 colour226 colour190 colour154 colour118 colour82 colour46"
#         ;;
#       heat)
#         palette="colour243 colour245 colour247 colour144 colour143 colour142 colour184 colour214 colour208 colour202 colour196"
#         ;;
#       gradient*)
#         palette=${palette_style##gradient(}
#         while [ $# -gt 0 ]; do
#           palette="$palette${1+ }${1%%)}"
#           [ "$1" != "${1%%)}" ] && break
#           shift
#         done
#         ;;
#     esac
#     bg=${2:-none}
#
#     palette=$(printf '%s' "$palette" | awk -v n="$bar_length" '{ for (i = 0; i < n - 1; ++i) printf "%s ", $(1 + i * NF / n); printf $NF }')
#     # shellcheck disable=SC2086
#     set -- $palette
#
#     full=$(awk "BEGIN { printf \"%.0f\", ($bar_value) * $bar_length }")
#     bar="#[bg=$bg]"
#     if [ "$full" -gt 0 ]; then
#       # shellcheck disable=SC2046
#       bar="$bar$(printf "#[fg=%s]$bar_symbol_full" $(printf '%s' "$palette" | cut -d' ' -f1-"$full"))"
#     fi
#     empty=$((bar_length - full))
#     if [ "$empty" -gt 0 ]; then
#       # shellcheck disable=SC2046
#       bar="$bar$(printf "#[fg=%s]$bar_symbol_empty" $(printf '%s' "$palette" | cut -d' ' -f$((full + 1))-$((full + empty))))"
#     fi
#     eval bar="$bar#[fg=\${$((full == 0 ? 1 : full))}]"
#   elif printf '%s' "$bar_palette" | grep -q -E '^(([#a-zA-Z0-9]{7,9}|none),?){3}$'; then
#     # shellcheck disable=SC2086
#     { set -f; IFS=,; set -- $bar_palette; unset IFS; set +f; }
#     full_fg=$1
#     empty_fg=$2
#     bg=${3:-none}
#
#     full=$(awk "BEGIN { printf \"%.0f\", ($bar_value) * $bar_length }")
#     bar="#[bg=$bg]"
#     if [ "$full" -gt 0 ]; then
#       bar="$bar#[fg=$full_fg]$(printf "%0.s$bar_symbol_full" $(seq 1 "$full"))"
#     fi
#     empty=$((bar_length - full))
#     if [ "$empty" -gt 0 ]; then
#       bar="$bar#[fg=$empty_fg]$(printf "%0.s$bar_symbol_empty" $(seq 1 "$empty"))#[fg=$empty_fg]"
#     fi
#   fi
#
#   printf '%s' "$bar"
# }
#
# _hbar() {
#   hbar_palette=$(printf '%s' "$1" | tr ';' ',')
#   hbar_value=$2
#
#   if printf '%s' "$hbar_palette" | grep -q -E '^(heat|gradient|gradient\((\s*[#a-zA-Z0-9]{7,9},?)+\))(\s*,\s*[#a-zA-Z0-9]{7,9})?$'; then
#     # shellcheck disable=SC2086
#     { set -f; IFS=', '; set -- $hbar_palette; unset IFS; set +f; }
#     palette_style=$1
#     shift
#
#     case "$palette_style" in
#       gradient)
#         palette="colour196 colour202 colour208 colour214 colour220 colour226 colour190 colour154 colour118 colour82 colour46"
#         ;;
#       heat)
#         palette="colour243 colour245 colour247 colour144 colour143 colour142 colour184 colour214 colour208 colour202 colour196"
#         ;;
#       gradient*)
#         palette=${palette_style##gradient(}
#         while [ $# -gt 0 ]; do
#           palette="$palette${1+ }${1%%)}"
#           [ "$1" != "${1%%)}" ] && break
#           shift
#         done
#         ;;
#     esac
#     bg=${2:-none}
#
#     palette=$(printf '%s' "$palette" | awk -v n=8 '{ for (i = 0; i < n - 1; ++i) printf "%s ", $(1 + i * NF / n); printf $NF }')
#     # shellcheck disable=SC2086
#     set -- $palette
#
#     full=$(awk "BEGIN { printf \"%.0f\", ($hbar_value) * 8 }")
#     eval hbar_fg="\${$((full == 0 ? 1 : full))}"
#   elif printf '%s' "$hbar_palette" | grep -q -E '^([#a-zA-Z0-9]{7,9},?){3}$'; then
#     # shellcheck disable=SC2086
#     { set -f; IFS=,; set -- $hbar_palette; unset IFS; set +f; }
#
#     # shellcheck disable=SC2046
#     eval $(awk "BEGIN { printf \"hbar_fg=$%d\", (($hbar_value) - 0.001) * $# + 1 }")
#   fi
#
#   set -- ▏ ▎ ▍ ▌ ▋ ▊ ▉ █
#   # shellcheck disable=SC2046
#   eval $(awk "BEGIN { printf \"hbar_symbol=$%d\", ($hbar_value) * ($# - 1) + 1 }")
#   hbar="#[bg=$bg]#[fg=${hbar_fg?}]${hbar_symbol?}"
#
#   printf '%s' "$hbar"
# }
#
# _vbar() {
#   vbar_palette=$(printf '%s' "$1" | tr ';' ',')
#   vbar_value=$2
#
#   if printf '%s' "$vbar_palette" | grep -q -E '^(heat|gradient|gradient\((\s*[#a-zA-Z0-9]{7,9},?)+\))(\s*,\s*[#a-zA-Z0-9]{7,9})?$'; then
#     # shellcheck disable=SC2086
#     { set -f; IFS=', '; set -- $vbar_palette; unset IFS; set +f; }
#     palette_style=$1
#     shift
#
#     case "$palette_style" in
#       gradient)
#         palette="colour196 colour202 colour208 colour214 colour220 colour226 colour190 colour154 colour118 colour82 colour46"
#         ;;
#       heat)
#         palette="colour243 colour245 colour247 colour144 colour143 colour142 colour184 colour214 colour208 colour202 colour196"
#         ;;
#       gradient*)
#         palette=${palette_style##gradient(}
#         while [ $# -gt 0 ]; do
#           palette="$palette${1+ }${1%%)}"
#           [ "$1" != "${1%%)}" ] && break
#           shift
#         done
#         ;;
#     esac
#     bg=${2:-none}
#
#     palette=$(printf '%s' "$palette" | awk -v n=8 '{ for (i = 0; i < n - 1; ++i) printf "%s ", $(1 + i * NF / n); printf $NF }')
#     # shellcheck disable=SC2086
#     set -- $palette
#
#     full=$(awk "BEGIN { printf \"%.0f\", ($vbar_value) * 8 }")
#     eval vbar_fg="\${$((full == 0 ? 1 : full))}"
#   elif printf '%s' "$vbar_palette" | grep -q -E '^([#a-zA-Z0-9]{7,9},?){3}$'; then
#     # shellcheck disable=SC2086
#     { set -f; IFS=,; set -- $vbar_palette; unset IFS; set +f; }
#
#     # shellcheck disable=SC2046
#     eval $(awk "BEGIN { printf \"vbar_fg=$%d\", (($vbar_value) - 0.001) * $# + 1 }")
#   fi
#
#   set -- ▁ ▂ ▃ ▄ ▅ ▆ ▇ █
#   # shellcheck disable=SC2046
#   eval $(awk "BEGIN { printf \"vbar_symbol=$%d\", ($vbar_value) * ($# - 1) + 1 }")
#   vbar="#[bg=$bg]#[fg=${vbar_fg?}]${vbar_symbol?}"
#
#   printf '%s' "$vbar"
# }
#
# _maximize_pane() {
#   current_session=${1:-$(tmux display -p '#{session_name}')}
#   current_pane=${2:-$(tmux display -p '#{pane_id}')}
#
#   dead_panes=$(tmux list-panes -s -t "$current_session" -F '#{pane_dead} #{pane_id} #{pane_start_command}' | grep -E -o '^1 %.+maximized.+$' || true)
#   restore=$(printf "%s" "$dead_panes" | sed -n -E -e "s/^1 $current_pane .+maximized.+'(%[0-9]+)'\"?$/tmux swap-pane -s \1 -t $current_pane \; kill-pane -t $current_pane/p"\
#                                            -e "s/^1 (%[0-9]+) .+maximized.+'$current_pane'\"?$/tmux swap-pane -s \1 -t $current_pane \; kill-pane -t \1/p")
#
#   if [ -z "$restore" ]; then
#     [ "$(tmux list-panes -t "$current_session:" | wc -l | sed 's/^ *//g')" -eq 1 ] && tmux display "Can't maximize with only one pane" && return
#     info=$(tmux new-window -t "$current_session:" -F "#{session_name}:#{window_index}.#{pane_id}" -P "maximized... 2>/dev/null & \"$TMUX_PROGRAM\" ${TMUX_SOCKET:+-S \"$TMUX_SOCKET\"} setw -t \"$current_session:\" remain-on-exit on; printf \"\\033[\$(tput lines);0fPane has been maximized, press <prefix>+ to restore\n\" '$current_pane'")
#     session_window=${info%.*}
#     new_pane=${info#*.}
#
#     retry=1000
#     while [ x"$(tmux list-panes -t "$session_window" -F '#{session_name}:#{window_index}.#{pane_id} #{pane_dead}' 2>/dev/null)" != x"$info 1" ] && [ "$retry" -ne 0 ]; do
#       sleep 0.1
#       retry=$((retry - 1))
#     done
#     if [ "$retry" -eq 0 ]; then
#       tmux display 'Unable to maximize pane'
#     fi
#
#     tmux setw -t "$session_window" remain-on-exit off \; swap-pane -s "$current_pane" -t "$new_pane"
#   else
#     $restore || tmux kill-pane
#   fi
# }
#
# _toggle_mouse() {
#   old=$(tmux show -gv mouse)
#   new=""
#
#   if [ "$old" = "on" ]; then
#     new="off"
#   else
#     new="on"
#   fi
#
#   tmux set -g mouse $new \; display "mouse: $new"
# }
#
# _fpp() {
#   tmux capture-pane -J -p -t "$1" | fpp -nfc -c "cd -- '$2' && exec \$SHELL"
# }
#
# _battery_info() {
#   count=0
#   charge=0
#   case "$_uname_s" in
#     *Darwin*)
#       while IFS= read -r line; do
#         [ -z "$line" ] && continue
#         discharging=$(printf '%s' "$line" | grep -qi "discharging" && echo "true" || echo "false")
#         percentage=$(printf '%s' "$line" | grep -E -o '[0-9]+%' || echo "0%")
#         charge=$(awk -v charge="$charge" -v percentage="${percentage%%%}" 'BEGIN { print charge + percentage / 100 }')
#         count=$((count + 1))
#       done  << EOF
# $(pmset -g batt | grep 'InternalBattery')
# EOF
#       ;;
#     *Linux*)
#       while IFS= read -r batpath; do
#         [ -z "$batpath" ] && continue
#         grep -i -q device "$batpath/scope" 2> /dev/null && continue
#
#         discharging=$(grep -qi "discharging" "$batpath/status" && echo "true" || echo "false")
#         bat_capacity="$batpath/capacity"
#         if [ -r "$bat_capacity" ]; then
#           charge=$(awk -v charge="$charge" -v capacity="$(cat "$bat_capacity")" 'BEGIN { print charge + (capacity > 100 ? 100 : capacity) / 100 }')
#         else
#           bat_energy_full="$batpath/energy_full"
#           bat_energy_now="$batpath/energy_now"
#           if [ -r "$bat_energy_full" ] && [ -r "$bat_energy_now" ]; then
#             charge=$(awk -v charge="$charge" -v energy_now="$(cat "$bat_energy_now")" -v energy_full="$(cat "$bat_energy_full")" 'BEGIN { print charge + energy_now / energy_full }')
#           fi
#         fi
#         count=$((count + 1))
#       done  << EOF
# $(find /sys/class/power_supply -maxdepth 1 -iname '*bat*')
# EOF
#       ;;
#     *CYGWIN*|*MSYS*|*MINGW*)
#       while IFS= read -r line; do
#         [ -z "$line" ] && continue
#         discharging=$(printf '%s' "$line" | awk '{ s = ($1 == 1) ? "true" : "false"; print s }')
#         charge=$(printf '%s' "$line" | awk -v charge="$charge" '{ print charge + $2 / 100 }')
#         count=$((count + 1))
#       done  << EOF
# $(wmic path Win32_Battery get BatteryStatus, EstimatedChargeRemaining 2> /dev/null | tr -d '\r' | tail -n +2 || true)
# EOF
#       ;;
#     *OpenBSD*)
#       for batid in 0 1 2; do
#         sysctl -n "hw.sensors.acpibat$batid.raw0" 2>&1 | grep -q 'not found' && continue
#         discharging=$(sysctl -n "hw.sensors.acpibat$batid.raw0" | grep -q 1 && echo "true" || echo "false")
#         if sysctl -n "hw.sensors.acpibat$batid" | grep -q amphour; then
#           charge=$(awk -v charge="$charge" -v remaining="$(sysctl -n hw.sensors.acpibat$batid.amphour3 | cut -d' ' -f1)" -v full="$(sysctl -n hw.sensors.acpibat$batid.amphour0 | cut -d' ' -f1)" 'BEGIN { print charge + remaining / full }')
#         else
#           charge=$(awk -v charge="$charge" -v remaining="$(sysctl -n hw.sensors.acpibat$batid.watthour3 | cut -d' ' -f1)" -v full="$(sysctl -n hw.sensors.acpibat$batid.watthour0 | cut -d' ' -f1)" 'BEGIN { print charge + remaining / full }')
#         fi
#         count=$((count + 1))
#       done
#       ;;
#   esac
#   [ "$count" -ne 0 ] && charge=$(awk -v charge="$charge" -v count="$count" 'BEGIN { print charge / count }') || true
# }
#
# _battery_status() {
#   _battery_info
#   if [ "$charge" = 0 ]; then
#     tmux set -ug '@battery_status'
#     return
#   fi
#
#   battery_status_charging=$1
#   battery_status_discharging=$2
#   if [ x"$discharging" = x"true" ]; then
#     battery_status="$battery_status_discharging"
#   else
#     battery_status="$battery_status_charging"
#   fi
#
#   tmux set -g '@battery_status' "$battery_status"
# }
#
# _battery_bar() {
#   _battery_info
#   if [ "$charge" = 0 ]; then
#     tmux  set -ug '@battery_bar'     \;\
#           set -ug '@battery_hbar'    \;\
#           set -ug '@battery_vbar'    \;\
#           set -ug '@battery_percentage'
#     return
#   fi
#
#   battery_bar_symbol_full=$1
#   battery_bar_symbol_empty=$2
#   battery_bar_length=$3
#   battery_bar_palette=$4
#   battery_hbar_palette=$5
#   battery_vbar_palette=$6
#
#   battery_bar=$(_bar "$battery_bar_palette" "$battery_bar_symbol_empty" "$battery_bar_symbol_full" "$battery_bar_length" "$charge")
#   battery_hbar=$(_hbar "$battery_hbar_palette" "$charge")
#   battery_vbar=$(_vbar "$battery_vbar_palette" "$charge")
#   battery_percentage="$(awk "BEGIN { printf \"%.0f%%\", ($charge) * 100 }")"
#
#   tmux  set -g '@battery_bar' "$battery_bar" \;\
#         set -g '@battery_hbar' "$battery_hbar" \;\
#         set -g '@battery_vbar' "$battery_vbar" \;\
#         set -g '@battery_percentage' "$battery_percentage"
# }
#
# _pane_info() {
#   pane_pid="$1"
#   pane_tty="${2##/dev/}"
#   case "$_uname_s" in
#     *CYGWIN*)
#       ps -al | tail -n +2 | awk -v pane_pid="$pane_pid" -v tty="$pane_tty" '
#         ((/ssh/ && !/-W/ && !/tsh proxy ssh/ && !/sss_ssh_knownhostsproxy/) || !/ssh/) && !/tee/ && $5 == tty {
#           user[$1] = $6; if (!child[$2]) child[$2] = $1
#         }
#         END {
#           pid = pane_pid
#           while (child[pid])
#             pid = child[pid]
#
#           file = "/proc/" pid "/cmdline"; getline command < file; close(file)
#           gsub(/\0/, " ", command)
#           "id -un " user[pid] | getline username
#           print pid":"username":"command
#         }
#       '
#       ;;
#     *Linux*)
#       ps -t "$pane_tty" --sort=lstart -o user=XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX -o pid= -o ppid= -o command= | awk -v pane_pid="$pane_pid" '
#         ((/ssh/ && !/-W/ && !/tsh proxy ssh/ && !/sss_ssh_knownhostsproxy/) || !/ssh/) && !/tee/ {
#           user[$2] = $1; if (!child[$3]) child[$3] = $2; pid visibleCommand = !command 2]
#           for (i = 4; i <= NF; ++i)
#           {
#             if ($i ~ /^-roles$/)
#               { ++i; continue }
#             if ($i ~ /^--(user|loginCommand)$/)
#               { ++i; continue }
#             if ($i !~ /^-/)
#               command[$2] = command[$2] ? command[$2] FS $i : $i
#           }
#         }
#         END {
#           pid = pane_pid
#           while (child[pid])
#             pid = child[pid]
#
#           print pid":"user[pid]":"command[pid]
#         }
#       '
#       ;;
#     *)
#       ps -t "$pane_tty" -o user=XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX -o pid= -o ppid= -o command= | awk -v pane_pid="$pane_pid" '
#         ((/ssh/ && !/-W/ && !/tsh proxy ssh/ && !/sss_ssh_knownhostsproxy/) || !/ssh/) && !/tee/ {
#           user[$2] = $1; if (!child[$3]) child[$3] = $2; command[$2] = ""
#           for (i = 4; i <= NF; ++i)
#           {
#             if ($i ~ /^-roles$/)
#               { ++i; continue }
#             if ($i ~ /^--(user|loginCommand)$/)
#               { ++i; continue }
#             if ($i !~ /^-/)
#               command[$2] = command[$2] ? command[$2] FS $i : $i
#           }
#         }
#         END {
#           pid = pane_pid
#           while (child[pid])
#             pid = child[pid]
#
#           print pid":"user[pid]":"command[pid]
#         }
#       '
#       ;;
#   esac
# }
#
# _ssh_or_mosh_args() {
#   case "$1" in
#     *ssh*)
#       args=$(printf '%s' "$1" | perl -n -e 'print if s/.*?\bssh[\w]*\s*(.*)/\1/')
#       ;;
#     *mosh-client*)
#       args=$(printf '%s' "$1" | sed -E -e 's/.*mosh-client -# (.*)\|.*$/\1/' -e 's/-[^ ]*//g' -e 's/\d:\d//g')
#       ;;
#   esac
#
#   printf '%s' "$args"
# }
#
# _username() {
#   pane_pid=${1:-$(tmux display -p '#{pane_pid}')}
#   pane_tty=${2:-$(tmux display -p '#{b:pane_tty}')}
#   ssh_only=$3
#
#   pane_info=$(_pane_info "$pane_pid" "$pane_tty")
#   command=${pane_info#*:}
#   command=${command#*:}
#
#   ssh_or_mosh_args=$(_ssh_or_mosh_args "$command")
#   if [ -n "$ssh_or_mosh_args" ]; then
#     # shellcheck disable=SC2086
#     username=$(ssh -G $ssh_or_mosh_args 2>/dev/null | awk '/^user / { print $2; exit }')
#     # shellcheck disable=SC2086
#     [ -z "$username" ] && username=$(ssh $ssh_or_mosh_args -T -o ControlPath=none -o ProxyCommand="echo %r; exit 1" 2>/dev/null || true)
#   else
#     if ! _is_true "$ssh_only"; then
#       username=${pane_info#*:}
#       username=${username%%:*}
#     fi
#   fi
#
#   printf '%s' "$username"
# }
#
# _hostname() {
#   pane_pid=${1:-$(tmux display -p '#{pane_pid}')}
#   pane_tty=${2:-$(tmux display -p '#{b:pane_tty}')}
#   ssh_only=$3
#   full=$4
#   h_or_H=$5
#
#   pane_info=$(_pane_info "$pane_pid" "$pane_tty")
#   command=${pane_info#*:}
#   command=${command#*:}
#
#   ssh_or_mosh_args=$(_ssh_or_mosh_args "$command")
#   if [ -n "$ssh_or_mosh_args" ]; then
#     # shellcheck disable=SC2086
#     hostname=$(ssh -G $ssh_or_mosh_args 2>/dev/null | awk '/^hostname / { print $2; exit }')
#     # shellcheck disable=SC2086
#     [ -z "$hostname" ] && hostname=$(ssh $ssh_or_mosh_args -T -o ControlPath=none -o ProxyCommand="echo %h; exit 1" 2>/dev/null || true)
#     #shellcheck disable=SC1stretchCase,SC2295
#     if ! _is_true "$full"; then
#       case "$hostname" in
#         *[a-z]*)
#           hostname=${hostname%%.*}
#           ;;
#         127.0.0.1)
#           hostname="localhost"
#           ;;
#       esac
#     fi
#   else
#     if ! _is_true "$ssh_only"; then
#       hostname="$h_or_H"
#     fi
#   fi
#
#   printf '%s' "$hostname"
# }
#
# _root() {
#   pane_pid=${1:-$(tmux display -p '#{pane_pid}')}
#   pane_tty=${2:-$(tmux display -p '#{b:pane_tty}')}
#   root=$3
#
#   username=$(_username "$pane_pid" "$pane_tty" false)
#
#   [ "$username" = "root" ] && echo "$root"
# }
#
# _uptime() {
#   case "$_uname_s" in
#     *Darwin*|*FreeBSD*)
#       boot=$(sysctl -n kern.boottime 2>/dev/null | awk -F'[ ,:]+' '{ print $4 }')
#       now=$(date +%s)
#       ;;
#     *Linux*|*CYGWIN*|*MSYS*|*MINGW*)
#       boot=0
#       now=$(cut -d' ' -f1 < /proc/uptime)
#       ;;
#     *OpenBSD*)
#       boot=$(sysctl -n kern.boottime)
#       now=$(date +%s)
#       ;;
#   esac
#   # shellcheck disable=SC1004
#   awk -v boot="$boot" -v now="$now" '
#     BEGIN {
#       uptime = now - boot
#       y = int(uptime / 31536000)
#       d = int(uptime % 31536000 / 86400)
#       h = int(uptime % 86400 / 3600)
#       m = int(uptime % 3600 / 60)
#       s = int(uptime % 60)
#
#       system("tmux  set -g @uptime_y " y + 0 " \\; " \
#                    "set -g @uptime_d " d + 0 " \\; " \
#                    "set -g @uptime_h " h + 0 " \\; " \
#                    "set -g @uptime_m " m + 0 " \\; " \
#                    "set -g @uptime_s " s + 0)
#     }'
# }
#
# _loadavg() {
#   case "$_uname_s" in
#     *Darwin*|*FreeBSD*)
#       tmux set -g @loadavg "$(sysctl -n vm.loadavg 2>/dev/null | cut -d' ' -f2)"
#       ;;
#     *Linux*|*CYGWIN*)
#       tmux set -g @loadavg "$(cut -d' ' -f1 < /proc/loadavg)"
#       ;;
#     *OpenBSD*)
#       tmux set -g @loadavg "$(sysctl -n vm.loadavg 2>/dev/null | cut -d' ' -f1)"
#       ;;
#   esac
# }
#
# _apply_plugins() {
#   tmux_conf_update_plugins_on_launch=${tmux_conf_update_plugins_on_launch:-true}
#   tmux_conf_update_plugins_on_reload=${tmux_conf_update_plugins_on_reload:-true}
#   tmux_conf_uninstall_plugins_on_reload=${tmux_conf_uninstall_plugins_on_reload:-true}
#
#   if [ -z "$TMUX_PLUGIN_MANAGER_PATH" ]; then
#     if [ -d ~/.tmux/plugins ]; then
#       TMUX_PLUGIN_MANAGER_PATH=~/.tmux/plugins
#     elif [ -d "${XDG_DATA_HOME:-$HOME/.local/share}"/tmux/plugins ]; then
#       TMUX_PLUGIN_MANAGER_PATH="${XDG_DATA_HOME:-$HOME/.local/share}"/tmux/plugins
#     else
#       TMUX_PLUGIN_MANAGER_PATH=~/.tmux/plugins
#     fi
#   fi
#
#   {
#     {
#       plugins=$(tmux show -gv @tpm_plugins 2>/dev/null)
#       if [ -z "$plugins" ]; then
#         plugins="$(cat <<EOF
# $(tmux show -gp | perl -n -e '/^\s*set(?:-option)?\s+(?:-\w+\s+)*@plugin\s+(.+)$/ && print "$1\n"')
# EOF
# )"
#       fi
#       plugins=$(printf '%s' "$plugins" | perl -p -e "s/(['\"])(.+)\\1/\\2/g" | tr ' ' '\n' | sort -u)
#
#       if [ "$(tmux show-environment -g TMUX_CONF_SHELL_CONFIGURATION 2>/dev/null)" = "TMUX_CONF_SHELL_CONFIGURATION=done" ]; then
#         if _is_true "$tmux_conf_update_plugins_on_reload"; then
#           update=true
#         fi
#         if _is_true "$tmux_conf_uninstall_plugins_on_reload" && [ -d "$TMUX_PLUGIN_MANAGER_PATH" ]; then
#           for d in "$TMUX_PLUGIN_MANAGER_PATH"/*/; do
#             [ -d "$d" ] || continue
#             installed_plugins=" $installed_plugins ${d##*plugins/}"
#           done
#           for plugin in $plugins; do
#             case "$plugin" in
#               *.git|*:*)
#                 p=${plugin%%.git}
#                 p=${p##*/}
#                 ;;
#               *)
#                 p=${plugin##*/}
#                 ;;
#             esac
#             installed_plugins=$(printf '%s' "$installed_plugins" | perl -p -e "s/ ${p}\\// /g")
#           done
#           for d in $installed_plugins; do
#             [ "$d" = ' ' ] && continue
#             rm -rf "$TMUX_PLUGIN_MANAGER_PATH/${d%/}"
#           done
#           unset installed_plugins
#         fi
#       else
#         if _is_true "$tmux_conf_update_plugins_on_launch"; then
#           update=true
#         fi
#       fi
#
#       for plugin in $plugins; do
#         case "$plugin" in
#           tmux-plugins/tpm|tpm)
#             continue
#             ;;
#         esac
#
#         case "$plugin" in
#           *.git)
#             plugin_basename=${plugin%%.git}
#             plugin_basename=${plugin_basename##*/}
#             [ -d "$TMUX_PLUGIN_MANAGER_PATH/$plugin_basename" ] || git clone --depth 1 "$plugin" "$TMUX_PLUGIN_MANAGER_PATH/$plugin_basename" &
#             ;;
#           *:*)
#             plugin_basename=${plugin#*:}
#             plugin_basename=${plugin_basename%%.git}
#             plugin_basename=${plugin_basename##*/}
#             [ -d "$TMUX_PLUGIN_MANAGER_PATH/$plugin_basename" ] || git clone --depth 1 "$plugin" "$TMUX_PLUGIN_MANAGER_PATH/$plugin_basename" &
#             ;;
#           *)
#             plugin_basename=${plugin##*/}
#             [ -d "$TMUX_PLUGIN_MANAGER_PATH/$plugin_basename" ] || git clone --depth 1 "https://github.com/$plugin" "$TMUX_PLUGIN_MANAGER_PATH/$plugin_basename" &
#             ;;
#         esac
#       done
#       wait
#
#       if [ -n "$update" ]; then
#         for d in "$TMUX_PLUGIN_MANAGER_PATH"/*/; do
#           [ -d "$d" ] || continue
#           ( cd "$d" && git pull && git submodule update --init --recursive ) &
#         done
#         wait
#       fi
#
#       for d in "$TMUX_PLUGIN_MANAGER_PATH"/*/; do
#         [ -d "$d" ] || continue
#         for f in "$d"*.tmux; do
#           [ -f "$f" ] || continue
#           ( cd "$d" && "$f" ) &
#         done
#       done
#       wait
#     } 2>&1 1>&3 3>&- | while IFS= read -r line; do printf '%s\n' "$line" | _timestamp; done 1>&2 3>&-
#   } 3>&1 >> "${TMUX_PLUGIN_MANAGER_PATH}/.tmux.log" 2>&1
# }
#
# _apply_theme() {
#
#   tmux_conf_theme=${tmux_conf_theme:-enabled}
#   if _is_disabled "$tmux_conf_theme"; then
#     return
#   fi
#
#   # -- pane border -------
#   tmux_conf_theme_pane_border=${tmux_conf_theme_pane_border:-#444444}
#   tmux_conf_theme_pane_active_border=${tmux_conf_theme_pane_active_border:-#00afff}
#   tmux set -g pane-border-style "fg=$tmux_conf_theme_pane_border" \; set -g pane-active-border-style "fg=$tmux_conf_theme_pane_active_border"
#
#   tmux_conf_theme_pane_border_style=${tmux_conf_theme_pane_border_style:-thin}
#   case "$tmux_conf_theme_pane_border_style" in
#     fat)
#       tmux set -gF pane-border-indicators 'colour'
#       ;;
#     thin|*)
#       tmux set -gF pane-border-indicators 'off'
#       ;;
#   esac
#
#   tmux_conf_theme_highlight_focused_pane=${tmux_conf_theme_highlight_focused_pane:-false}
#   if _is_true "$tmux_conf_theme_highlight_focused_pane"; then
#     tmux_conf_theme_focused_pane_bg=${tmux_conf_theme_focused_pane_bg:-#161414}
#     tmux set -g window-active-style "bg=$tmux_conf_theme_focused_pane_bg"
#   else
#     tmux set -g window-active-style 'default'
#   fi
#
#   tmux_conf_theme_window_fg=${tmux_conf_theme_window_fg:-default}
#   tmux_conf_theme_window_bg=${tmux_conf_theme_window_bg:-default}
#   tmux set -g window-style "fg=$tmux_conf_theme_window_fg,bg=$tmux_conf_theme_window_bg"
#
#   # -- pane indicator -------
#   tmux_conf_theme_pane_indicator=${tmux_conf_theme_pane_indicator:-#00afff}
#   tmux_conf_theme_pane_active_indicator=${tmux_conf_theme_pane_active_indicator:-#00afff}
#   tmux set -g display-panes-colour "$tmux_conf_theme_pane_indicator" \; set -g display-panes-active-colour "$tmux_conf_theme_pane_active_indicator"
#
#   # -- modes -------
#   tmux_conf_theme_mode_fg=${tmux_conf_theme_mode_fg:-#000000}
#   tmux_conf_theme_mode_bg=${tmux_conf_theme_mode_bg:-#ffff00}
#   tmux_conf_theme_mode_attr=${tmux_conf_theme_mode_attr:-bold}
#   tmux setw -g mode-style "fg=$tmux_conf_theme_mode_fg,bg=$tmux_conf_theme_mode_bg,$tmux_conf_theme_mode_attr"
#
#   # -- message -------
#   tmux_conf_theme_message_fg=${tmux_conf_theme_message_fg:-#000000}
#   tmux_conf_theme_message_bg=${tmux_conf_theme_message_bg:-#ffff00}
#   tmux_conf_theme_message_attr=${tmux_conf_theme_message_attr:-bold}
#   tmux set -g message-style "fg=$tmux_conf_theme_message_fg,bg=$tmux_conf_theme_message_bg,$tmux_conf_theme_message_attr"
#
#   tmux_conf_theme_message_command_fg=${tmux_conf_theme_message_command_fg:-#ffff00}
#   tmux_conf_theme_message_command_bg=${tmux_conf_theme_message_command_bg:-#000000}
#   tmux_conf_theme_message_command_attr=${tmux_conf_theme_message_command_attr:-bold}
#   tmux set -g message-command-style "fg=$tmux_conf_theme_message_command_fg,bg=$tmux_conf_theme_message_command_bg,$tmux_conf_theme_message_command_attr"
#
#   # -- status line -------
#   tmux_conf_theme_status_fg=${tmux_conf_theme_status_fg:-#8a8a8a}
#   tmux_conf_theme_status_bg=${tmux_conf_theme_status_bg:-#080808}
#   tmux_conf_theme_status_attr=${tmux_conf_theme_status_attr:-none}
#   tmux set -g status-style "fg=$tmux_conf_theme_status_fg,bg=$tmux_conf_theme_status_bg,$tmux_conf_theme_status_attr"
#
#   tmux_conf_theme_terminal_title=${tmux_conf_theme_terminal_title:-#h ❐ #S ● #I #W}
#
#   tmux_conf_theme_window_status_fg=${tmux_conf_theme_window_status_fg:-#8a8a8a}
#   tmux_conf_theme_window_status_bg=${tmux_conf_theme_window_status_bg:-#080808}
#   tmux_conf_theme_window_status_attr=${tmux_conf_theme_window_status_attr:-none}
#   tmux_conf_theme_window_status_format=${tmux_conf_theme_window_status_format:-'#I #W#{?#{||:#{window_bell_flag},#{window_zoomed_flag}}, ,}#{?window_bell_flag,!,}#{?window_zoomed_flag,Z,}'}
#
#   tmux_conf_theme_window_status_current_fg=${tmux_conf_theme_window_status_current_fg:-#000000}
#   tmux_conf_theme_window_status_current_bg=${tmux_conf_theme_window_status_current_bg:-#00afff}
#   tmux_conf_theme_window_status_current_attr=${tmux_conf_theme_window_status_current_attr:-bold}
#   tmux_conf_theme_window_status_current_format=${tmux_conf_theme_window_status_current_format:-'#I #W#{?#{||:#{window_bell_flag},#{window_zoomed_flag}}, ,}#{?window_bell_flag,!,}#{?window_zoomed_flag,Z,}'}
#
#   tmux_conf_theme_window_status_activity_fg=${tmux_conf_theme_window_status_activity_fg:-default}
#   tmux_conf_theme_window_status_activity_bg=${tmux_conf_theme_window_status_activity_bg:-default}
#   tmux_conf_theme_window_status_activity_attr=${tmux_conf_theme_window_status_activity_attr:-underscore}
#
#   tmux_conf_theme_window_status_bell_fg=${tmux_conf_theme_window_status_bell_fg:-#ffff00}
#   tmux_conf_theme_window_status_bell_bg=${tmux_conf_theme_window_status_bell_bg:-default}
#   tmux_conf_theme_window_status_bell_attr=${tmux_conf_theme_window_status_bell_attr:-blink,bold}
#
#   tmux_conf_theme_window_status_last_fg=${tmux_conf_theme_window_status_last_fg:-#00afff}
#   tmux_conf_theme_window_status_last_bg=${tmux_conf_theme_window_status_last_bg:-#232323}
#   tmux_conf_theme_window_status_last_attr=${tmux_conf_theme_window_status_last_attr:-none}
#
#   tmux_conf_theme_left_separator_main=${tmux_conf_theme_left_separator_main-''}
#   tmux_conf_theme_left_separator_sub=${tmux_conf_theme_left_separator_sub-'|'}
#   tmux_conf_theme_right_separator_main=${tmux_conf_theme_right_separator_main-''}
#   tmux_conf_theme_right_separator_sub=${tmux_conf_theme_right_separator_sub-'|'}
#
#   tmux_conf_theme_status_left=${tmux_conf_theme_status_left-' ❐ #S | ↑#{?uptime_y, #{uptime_y}y,}#{?uptime_d, #{uptime_d}d,}#{?uptime_h, #{uptime_h}h,}#{?uptime_m, #{uptime_m}m,} '}
#   tmux_conf_theme_status_right=${tmux_conf_theme_status_right-' #{prefix}#{mouse}#{pairing}#{synchronized}#{?battery_status,#{battery_status},}#{?battery_bar, #{battery_bar},}#{?battery_percentage, #{battery_percentage},} , %R , %d %b | #{username}#{root} | #{hostname} '}
#
#   tmux_conf_theme_status_left_fg=${tmux_conf_theme_status_left_fg:-'#000000,#e4e4e4,#000000'}
#   tmux_conf_theme_status_left_bg=${tmux_conf_theme_status_left_bg:-'#ffff00,#ff00af,#00afff'}
#   tmux_conf_theme_status_left_attr=${tmux_conf_theme_status_left_attr:-'bold,none,none'}
#   tmux_conf_theme_status_right_fg=${tmux_conf_theme_status_right_fg:-'#8a8a8a,#e4e4e4,#000000'}
#   tmux_conf_theme_status_right_bg=${tmux_conf_theme_status_right_bg:-'#080808,#d70000,#e4e4e4'}
#   tmux_conf_theme_status_right_attr=${tmux_conf_theme_status_right_attr:-'none,none,bold'}
#
#   tmux_conf_theme_clock_colour=${tmux_conf_theme_clock_colour:-#00afff}
#   tmux_conf_theme_clock_style=${tmux_conf_theme_clock_style:-24}
#   tmux set -g clock-mode-colour "$tmux_conf_theme_clock_colour" \; set -g clock-mode-style "$tmux_conf_theme_clock_style"
#
#   _pkillf 'sleep [0-9]* ; export VISUAL'
#   tmux_conf_theme_pairing=${tmux_conf_theme_pairing:-⚇}
#   tmux_conf_theme_pairing_fg=${tmux_conf_theme_pairing_fg:-none}
#   tmux_conf_theme_pairing_bg=${tmux_conf_theme_pairing_bg:-none}
#   tmux_conf_theme_pairing_attr=${tmux_conf_theme_pairing_attr:-none}
#
#   tmux_conf_theme_prefix=${tmux_conf_theme_prefix:-⌨}
#   tmux_conf_theme_prefix_fg=${tmux_conf_theme_prefix_fg:-none}
#   tmux_conf_theme_prefix_bg=${tmux_conf_theme_prefix_bg:-none}
#   tmux_conf_theme_prefix_attr=${tmux_conf_theme_prefix_attr:-none}
#
#   tmux_conf_theme_mouse=${tmux_conf_theme_mouse:-↗}
#   tmux_conf_theme_mouse_fg=${tmux_conf_theme_mouse_fg:-none}
#   tmux_conf_theme_mouse_bg=${tmux_conf_theme_mouse_bg:-none}
#   tmux_conf_theme_mouse_attr=${tmux_conf_theme_mouse_attr:-none}
#
#   tmux_conf_theme_root=${tmux_conf_theme_root:-!}
#   tmux_conf_theme_root_fg=${tmux_conf_theme_root_fg:-none}
#   tmux_conf_theme_root_bg=${tmux_conf_theme_root_bg:-none}
#   tmux_conf_theme_root_attr=${tmux_conf_theme_root_attr:-bold,blink}
#
#   tmux_conf_theme_synchronized=${tmux_conf_theme_synchronized:-⚏}
#   tmux_conf_theme_synchronized_fg=${tmux_conf_theme_synchronized_fg:-none}
#   tmux_conf_theme_synchronized_bg=${tmux_conf_theme_synchronized_bg:-none}
#   tmux_conf_theme_synchronized_attr=${tmux_conf_theme_synchronized_attr:-none}
#
#   tmux_conf_battery_bar_symbol_full=${tmux_conf_battery_bar_symbol_full:-◼}
#   tmux_conf_battery_bar_symbol_empty=${tmux_conf_battery_bar_symbol_empty:-◻}
#   tmux_conf_battery_bar_length=${tmux_conf_battery_bar_length:-auto}
#   tmux_conf_battery_bar_palette=${tmux_conf_battery_bar_palette:-gradient}
#   tmux_conf_battery_hbar_palette=${tmux_conf_battery_hbar_palette:-gradient}
#   tmux_conf_battery_vbar_palette=${tmux_conf_battery_vbar_palette:-gradient}
#   tmux_conf_battery_status_charging=${tmux_conf_battery_status_charging:-↑}
#   tmux_conf_battery_status_discharging=${tmux_conf_battery_status_discharging:-↓}
#
#   status_left=$(perl -p -e '
#     ; s/#\{(\?)?pairing}/#\{$1@pairing}/g
#     ; s/#\{(\?)?prefix}/#\{$1@prefix}/g
#     ; s/#\{(\?)?mouse}/#\{$1@mouse}/g
#     ; s/#\{(\?)?synchronized}/#\{$1@synchronized}/g
#     ; s/#\{(\?)?root}/#\{$1@root}/g
#     ; s/#\{(\?)?battery_bar}/#\{$1@battery_bar}/g
#     ; s/#\{(\?)?battery_hbar}/#\{$1@battery_hbar}/g
#     ; s/#\{(\?)?battery_vbar}/#\{$1@battery_vbar}/g
#     ; s/#\{(\?)?battery_percentage}/#\{$1@battery_percentage}/g
#     ; s/#\{(\?)?battery_status}/#\{$1@battery_status}/g
#     ; s/#\{(\?)?uptime_y}/#\{$1@uptime_y}/g
#     ; s/#\{(\?)?uptime_d}/#\{$1@uptime_d}/g
#     ; s/#\{(\?)?uptime_h}/#\{$1@uptime_h}/g
#     ; s/#\{(\?)?uptime_m}/#\{$1@uptime_m}/g
#     ; s/#\{(\?)?uptime_s}/#\{$1@uptime_s}/g
#     ; s/#\{(\?)?loadavg}/#\{$1@loadavg}/g
#     ; s/#\{hostname_full}/#H/g
#     ; s/#\{hostname}/#h/g
#     ; s/#\{hostname_full_ssh}/#\{?SSH_CONNECTION,#H,}/g
#     ; s/#\{hostname_ssh}/#\{?SSH_CONNECTION,#h,}/g
#     ; s/#\{username}/#(cut -c3- ~\/.tmux.conf | sh -s _username #\{pane_pid} #\{b:pane_tty} false #D)/g
#     ; s/#\{username_ssh}/#(cut -c3- ~\/.tmux.conf | sh -s _username #\{pane_pid} #\{b:pane_tty} true #D)/g
#     ; s/#\{circled_session_name}/#(cut -c3- ~\/.tmux.conf | sh -s _circled #S)/g' << EOF
# $tmux_conf_theme_status_left
# EOF
# )
#
#   status_right=$(perl -p -e '
#     ; s/#\{(\?)?pairing}/#\{$1@pairing}/g
#     ; s/#\{(\?)?prefix}/#\{$1@prefix}/g
#     ; s/#\{(\?)?mouse}/#\{$1@mouse}/g
#     ; s/#\{(\?)?synchronized}/#\{$1@synchronized}/g
#     ; s/#\{(\?)?root}/#\{$1@root}/g
#     ; s/#\{(\?)?battery_bar}/#\{$1@battery_bar}/g
#     ; s/#\{(\?)?battery_hbar}/#\{$1@battery_hbar}/g
#     ; s/#\{(\?)?battery_vbar}/#\{$1@battery_vbar}/g
#     ; s/#\{(\?)?battery_percentage}/#\{$1@battery_percentage}/g
#     ; s/#\{(\?)?battery_status}/#\{$1@battery_status}/g
#     ; s/#\{(\?)?uptime_y}/#\{$1@uptime_y}/g
#     ; s/#\{(\?)?uptime_d}/#\{$1@uptime_d}/g
#     ; s/#\{(\?)?uptime_h}/#\{$1@uptime_h}/g
#     ; s/#\{(\?)?uptime_m}/#\{$1@uptime_m}/g
#     ; s/#\{(\?)?uptime_s}/#\{$1@uptime_s}/g
#     ; s/#\{(\?)?loadavg}/#\{$1@loadavg}/g
#     ; s/#\{hostname_full}/#H/g
#     ; s/#\{hostname}/#h/g
#     ; s/#\{hostname_full_ssh}/#\{?SSH_CONNECTION,#H,}/g
#     ; s/#\{hostname_ssh}/#\{?SSH_CONNECTION,#h,}/g
#     ; s/#\{username}/#(cut -c3- ~\/.tmux.conf | sh -s _username #\{pane_pid} #\{b:pane_tty} false #D)/g
#     ; s/#\{username_ssh}/#(cut -c3- ~\/.tmux.conf | sh -s _username #\{pane_pid} #\{b:pane_tty} true #D)/g
#     ; s/#\{circled_session_name}/#(cut -c3- ~\/.tmux.conf | sh -s _circled #S)/g' << EOF
# $tmux_conf_theme_status_right
# EOF
# )
#
#   window_status_format=$(perl -p -e '
#     ; s/#\{circled_window_index}/#(cut -c3- ~\/.tmux.conf | sh -s _circled #I)/g' << EOF
# $tmux_conf_theme_window_status_format
# EOF
# )
#
#   window_status_current_format=$(perl -p -e '
#     ; s/#\{circled_window_index}/#(cut -c3- ~\/.tmux.conf | sh -s _circled #I)/g' << EOF
# $tmux_conf_theme_window_status_current_format
# EOF
# )
#
#   tmux_conf_theme_left_separator_main=$(_decode_unicode_escapes "$tmux_conf_theme_left_separator_main")
#   tmux_conf_theme_left_separator_sub=$(_decode_unicode_escapes "$tmux_conf_theme_left_separator_sub")
#   tmux_conf_theme_right_separator_main=$(_decode_unicode_escapes "$tmux_conf_theme_right_separator_main")
#   tmux_conf_theme_right_separator_sub=$(_decode_unicode_escapes "$tmux_conf_theme_right_separator_sub")
#
#   _status_left=$(awk \
#                       -v fg_="$tmux_conf_theme_status_left_fg" \
#                       -v bg_="$tmux_conf_theme_status_left_bg" \
#                       -v attr_="$tmux_conf_theme_status_left_attr" \
#                       -v sep_main="$tmux_conf_theme_left_separator_main" \
#                       -v sep_sub="$tmux_conf_theme_left_separator_sub" \
#                       -v mainsep_bg="$tmux_conf_theme_status_bg" '
#     function subsplit(s,     l, i, a, r) {
#       l = split(s, a, ",")
#       for (i = 1; i <= l; ++i)
#       {
#         if (i > 1) r = r ","
#         r = r gensub(/,/, "‚", "g", a[i])
#       }
#       return r
#     }
#     function main(s, bg, default_, s_,     l, i, a, r, fg, sep, mainsep, subsep, first_subseg) {
#       l = split(s, a, "|")
#       for (i = 1; i <= l; ++i)
#       {
#         if (a[i] == "")
#           continue
#         first_subseg = 1
#         fg = default_
#         sub(/\s+/, "", a[i])
#         s_ = subsplit(a[i])
#         split(s_, b, ",")
#         for (j = 1; j <= length(b); ++j)
#         {
#           if (b[j] == "")
#             continue
#           sep = (first_subseg) ? mainsep : subsep
#           first_subseg = 0
#           r = r sprintf("%s%s", s_, sep)
#         }
#       }
#       return r
#     }
#     BEGIN {
#       FS = OFS = ""
#       l_fg = split(fg_, fg, ",")
#       l_bg = split(bg_, bg, ",")
#       l_attr = split(attr_, attr, ",")
#       # left status line
#       l = split("'"$status_left"'", status_left, "|")
#       for (i = 1; i <= l; ++i)
#       {
#         fg_ = (i <= l_fg) ? fg[i] : fg[l_fg]
#         bg_ = (i <= l_bg) ? bg[i] : bg[l_bg]
#         attr_ = (i <= l_attr) ? attr[i] : attr[l_attr]
#         mainsep_fg = ((i + 1) <= l_bg) ? bg[i + 1] : mainsep_bg
#
#         if (bg_ == "none") bg_ = mainsep_bg
#         gsub(/,/, "‚", status_left[i])
#         gsub(/#\[([^\]]+)\]/, "‹\\1›", status_left[i])
#
#         subsplit(status_left[i], subseg)
#         l2 = split(status_left[i], subseg, ",")
#         for (j = 1; j <= l2; ++j)
#         {
#           if (subseg[j] == "")
#             continue
#           sep = (j == 1) ? sprintf("#[fg=%s,bg=%s,none]%s", bg_, mainsep_fg, sep_main) : sprintf("#[fg=%s,bg=%s,%s]%s", fg_, bg_, attr_, sep_sub)
#           r = sprintf("%s%s#[fg=%s,bg=%s,%s]%s", r, sep, fg_, bg_, attr_, subseg[j])
#         }
#         mainsep_bg = bg_
#       }
#       gsub(/‹/, "#[", r)
#       gsub(/›/, "]", r)
#       gsub(/‚/, ",", r)
#       print r "#[fg=" mainsep_bg ",bg=" mainsep_fg ",none]" sep_main
#     }')
#
#   _status_right=$(awk \
#                        -v fg_="$tmux_conf_theme_status_right_fg" \
#                        -v bg_="$tmux_conf_theme_status_right_bg" \
#                        -v attr_="$tmux_conf_theme_status_right_attr" \
#                        -v sep_main="$tmux_conf_theme_right_separator_main" \
#                        -v sep_sub="$tmux_conf_theme_right_separator_sub" \
#                        -v mainsep_bg="$tmux_conf_theme_status_bg" '
#     function subsplit(s,     l, i, a, r) {
#       l = split(s, a, ",")
#       for (i = 1; i <= l; ++i)
#       {
#         if (i > 1) r = r ","
#         r = r gensub(/,/, "‚", "g", a[i])
#       }
#       return r
#     }
#     BEGIN {
#       FS = OFS = ""
#       l_fg = split(fg_, fg, ",")
#       l_bg = split(bg_, bg, ",")
#       l_attr = split(attr_, attr, ",")
#       # right status line
#       l = split("'"$status_right"'", status_right, "|")
#       for (i = 1; i <= l; ++i)
#       {
#         fg_ = (i <= l_fg) ? fg[i] : fg[l_fg]
#         bg_ = (i <= l_bg) ? bg[i] : bg[l_bg]
#         attr_ = (i <= l_attr) ? attr[i] : attr[l_attr]
#         if (bg_ == "none") bg_ = mainsep_bg
#         gsub(/,/, "‚", status_right[i])
#         gsub(/#\[([^\]]+)\]/, "‹\\1›", status_right[i])
#
#         l2 = split(status_right[i], subseg, ",")
#         for (j = 1; j <= l2; ++j)
#         {
#           if (subseg[j] == "")
#             continue
#           sep = (j == 1) ? sprintf("#[fg=%s,bg=%s,none]%s", bg_, mainsep_bg, sep_main) : sprintf("#[fg=%s,bg=%s,%s]%s", fg_, bg_, attr_, sep_sub)
#           r = sprintf("%s%s#[fg=%s,bg=%s,%s]%s", r, sep, fg_, bg_, attr_, subseg[j])
#         }
#         mainsep_bg = bg_
#       }
#       gsub(/‹/, "#[", r)
#       gsub(/›/, "]", r)
#       gsub(/‚/, ",", r)
#       print r
#     }')
#
#   _window_status_format=$(awk \
#                                -v fg="$tmux_conf_theme_window_status_fg" \
#                                -v bg="$tmux_conf_theme_window_status_bg" \
#                                -v attr="$tmux_conf_theme_window_status_attr" '
#     BEGIN {
#       printf "#[fg=%s,bg=%s,%s]%s", fg, bg, attr, "'"$window_status_format"'"
#     }')
#
#   _window_status_current_format=$(awk \
#                                        -v fg="$tmux_conf_theme_window_status_current_fg" \
#                                        -v bg="$tmux_conf_theme_window_status_current_bg" \
#                                        -v attr="$tmux_conf_theme_window_status_current_attr" \
#                                        -v sep_main="$tmux_conf_theme_left_separator_main" \
#                                        -v mainsep_bg="$tmux_conf_theme_status_bg" '
#     BEGIN {
#       printf "#[fg=%s,bg=%s,none]%s#[fg=%s,bg=%s,%s]%s#[fg=%s,bg=%s,none]%s", mainsep_bg, bg, sep_main, fg, bg, attr, "'"$window_status_current_format"'", bg, mainsep_bg, sep_main
#     }')
#
#   # -- indicators
#
#   tmux set -g '@pairing' "#{?session_many_attached,$tmux_conf_theme_pairing,}" \;\
#        set -g '@prefix' "#{?client_prefix,$tmux_conf_theme_prefix,}" \;\
#        set -g '@mouse' "#{?mouse,$tmux_conf_theme_mouse,}" \;\
#        set -g '@root' "#{?#{==:#(cut -c3- ~/.tmux.conf | sh -s _username #{pane_pid} #{b:pane_tty} false #D),root},$tmux_conf_theme_root,}" \;\
#        set -g '@synchronized' "#{?pane_synchronized,$tmux_conf_theme_synchronized,}"
#
#   # -- terminal title
#   tmux set -g set-titles-string "$(_decode_unicode_escapes "$tmux_conf_theme_terminal_title")"
#
#   # -- window status
#   tmux setw -g window-status-style "fg=$tmux_conf_theme_window_status_fg,bg=$tmux_conf_theme_window_status_bg,$tmux_conf_theme_window_status_attr" \;\
#        setw -g window-status-format "$(_decode_unicode_escapes "$_window_status_format")" \;\
#        setw -g window-status-current-style "fg=$tmux_conf_theme_window_status_current_fg,bg=$tmux_conf_theme_window_status_current_bg,$tmux_conf_theme_window_status_current_attr" \;\
#        setw -g window-status-current-format "$(_decode_unicode_escapes "$_window_status_current_format")" \;\
#        setw -g window-status-activity-style "fg=$tmux_conf_theme_window_status_activity_fg,bg=$tmux_conf_theme_window_status_activity_bg,$tmux_conf_theme_window_status_activity_attr" \;\
#        setw -g window-status-bell-style "fg=$tmux_conf_theme_window_status_bell_fg,bg=$tmux_conf_theme_window_status_bell_bg,$tmux_conf_theme_window_status_bell_attr" \;\
#        setw -g window-status-last-style "fg=$tmux_conf_theme_window_status_last_fg,bg=$tmux_conf_theme_window_status_last_bg,$tmux_conf_theme_window_status_last_attr"
#
#   # -- status left & right
#   tmux set -g status-left-length 1000 \;\
#        set -g status-left "$(_decode_unicode_escapes "$_status_left")" \;\
#        set -g status-right-length 1000 \;\
#        set -g status-right "$(_decode_unicode_escapes "$_status_right")"
# }
#
# _apply_bindings() {
#   tmux_conf_preserve_stock_bindings=${tmux_conf_preserve_stock_bindings:-false}
#   if _is_true "$tmux_conf_preserve_stock_bindings"; then
#     return
#   fi
#
#   tmux_conf_new_window_retain_current_path=${tmux_conf_new_window_retain_current_path:-false}
#   tmux_conf_new_window_reconnect_ssh=${tmux_conf_new_window_reconnect_ssh:-false}
#   if ! _is_disabled "$tmux_conf_new_window_retain_current_path"; then
#     if _is_true "$tmux_conf_new_window_retain_current_path"; then
#       new_window='-c "#{pane_current_path}"'
#     fi
#   fi
#   if ! _is_disabled "$tmux_conf_new_window_reconnect_ssh"; then
#     if _is_true "$tmux_conf_new_window_reconnect_ssh"; then
#       new_window="$new_window \"#{?#{==:#(cut -c3- '#{TMUX_CONF}' | sh -s _ssh_aware),ssh},sh -c 'eval \\\"\$(cut -c3- \\\"#{TMUX_CONF}\\\" | sh -s _reconnect_ssh \\\"#{pane_pid}\\\" \\\"#{b:pane_tty}\\\")\\\"',}\""
#     fi
#   fi
#   if [ -n "$new_window" ]; then
#     tmux bind c new-window $new_window
#   fi
#
#   tmux_conf_new_pane_retain_current_path=${tmux_conf_new_pane_retain_current_path:-true}
#   tmux_conf_new_pane_reconnect_ssh=${tmux_conf_new_pane_reconnect_ssh:-false}
#   if ! _is_disabled "$tmux_conf_new_pane_retain_current_path"; then
#     if _is_true "$tmux_conf_new_pane_retain_current_path"; then
#       new_pane='-c "#{pane_current_path}"'
#     fi
#   fi
#   if ! _is_disabled "$tmux_conf_new_pane_reconnect_ssh"; then
#     if _is_true "$tmux_conf_new_pane_reconnect_ssh"; then
#       new_pane="$new_pane \"#{?#{==:#(cut -c3- '#{TMUX_CONF}' | sh -s _ssh_aware),ssh},sh -c 'eval \\\"\$(cut -c3- \\\"#{TMUX_CONF}\\\" | sh -s _reconnect_ssh \\\"#{pane_pid}\\\" \\\"#{b:pane_tty}\\\")\\\"',}\""
#     fi
#   fi
#   if [ -n "$new_pane" ]; then
#     tmux bind - split-window -v $new_pane \;\
#          bind _ split-window -h $new_pane \;\
#          bind '%' split-window -h $new_pane \;\
#          bind '"' split-window -v $new_pane
#   fi
#
#   tmux_conf_new_session_prompt=${tmux_conf_new_session_prompt:-false}
#   tmux_conf_new_session_retain_current_path=${tmux_conf_new_session_retain_current_path:-false}
#   if ! _is_disabled "$tmux_conf_new_session_prompt" && ! _is_disabled "$tmux_conf_new_session_retain_current_path"; then
#     if _is_true "$tmux_conf_new_session_prompt"; then
#       if _is_true "$tmux_conf_new_session_retain_current_path"; then
#         tmux bind C-c command-prompt -p new-session "new-session -c '#{pane_current_path}' -s '%%'"
#       else
#         tmux bind C-c command-prompt -p new-session "new-session -s '%%'"
#       fi
#     else
#       if _is_true "$tmux_conf_new_session_retain_current_path"; then
#         tmux bind C-c new-session -c '#{pane_current_path}'
#       fi
#     fi
#   fi
# }
#
# _apply_configuration() {
#
#   tmux_conf_24b_colour=${tmux_conf_24b_colour:-auto}
#   if [ "$tmux_conf_24b_colour" = "auto" ]; then
#     if [ "$COLORTERM" = "truecolor" ] || [ "$COLORTERM" = "24bit" ] || [ "$(tput colors 2>/dev/null)" = "16777216" ]; then
#       tmux_conf_24b_colour=true
#     else
#       tmux_conf_24b_colour=false
#     fi
#   fi
#   if _is_true "$tmux_conf_24b_colour"; then
#     case "$TERM" in
#       screen-*|tmux-*)
#         [ "$_tmux_version" -ge 3002 ] && tmux set -s terminal-features 'xterm*:RGB'
#         ;;
#       *)
#         ;;
#     esac
#   fi
#
#   _apply_theme
#   _apply_bindings
#   for f in $(tmux show -gvq '@tmux_conf_hooks'); do
#     $f || true
#   done
#   tmux set-environment -g TMUX_CONF_SHELL_CONFIGURATION "done"
#
#   _uptime
#   _loadavg &
#   _battery_bar "$tmux_conf_battery_bar_symbol_full" "$tmux_conf_battery_bar_symbol_empty" "$tmux_conf_battery_bar_length" "$tmux_conf_battery_bar_palette" "$tmux_conf_battery_hbar_palette" "$tmux_conf_battery_vbar_palette" &
#   _battery_status "$tmux_conf_battery_status_charging" "$tmux_conf_battery_status_discharging" &
#   wait
#
#   _apply_plugins
# }
#
# _ssh_aware() {
#   pane_pid=$1
#   pane_tty=${2##/dev/}
#
#   pane_info=$(_pane_info "$pane_pid" "$pane_tty")
#   command=${pane_info#*:}
#   command=${command#*:}
#
#   case "$command" in
#     *ssh*|*mosh-client*)
#       echo ssh
#       ;;
#   esac
# }
#
# _reconnect_ssh() {
#   pane_pid=$1
#   pane_tty=${2##/dev/}
#
#   pane_info=$(_pane_info "$pane_pid" "$pane_tty")
#   command=${pane_info#*:}
#   command=${command#*:}
#
#   case "$command" in
#     *ssh*)
#       # shellcheck disable=SC2046,SC2086
#       set -- $(printf '%s' "$command" | perl -n -e 'print if s/.*?\bssh[\w]*\s*(.*)/\1/')
#       while [ $# -gt 0 ]; do
#         case "$1" in
#           -*)
#             shift
#             case "$1" in
#               -*)
#                 ;;
#               *)
#                 shift
#                 ;;
#             esac
#             ;;
#           *)
#             break
#             ;;
#         esac
#       done
#       printf 'exec ssh "%s"' "$*"
#       ;;
#     *mosh-client*)
#       printf '%s' "$command"
#       ;;
#   esac
# }
#
# "$@"
# # /!\ do not remove the previous line
# #     do not write below this line
